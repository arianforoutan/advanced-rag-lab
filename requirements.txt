# Runtime dependencies for the Insurellm RAG subproject.
# These are a subset of the parent workspace's ../pyproject.toml. If you use the
# workspace's uv environment (`uv sync` in the parent dir), you do not need this
# file; it is provided for installing this subproject standalone:
#     pip install -r requirements.txt

langchain>=1.3.14
langchain-openai>=1.4.1
langchain-community>=0.4.2
langchain-chroma>=1.1.0
langchain-huggingface>=1.2.2
langchain-neo4j>=0.10.0
chromadb>=1.5.9
neo4j>=6.2.0
sentence-transformers>=5.6.1
rank-bm25>=0.2.2
python-dotenv>=1.2.2
langsmith
faiss-cpu>=1.8.0
# Evaluation (scripts/evaluate.py). ragas provides the end-to-end LLM-as-judge
# metrics; datasets is its input format. See rag/evaluation/_compat.py for the
# langchain-community compatibility shim ragas 0.4.x needs.
ragas>=0.4.3
datasets>=5.0.1
